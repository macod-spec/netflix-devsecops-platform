name: Deploy Dev to AKS

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  ACR_LOGIN_SERVER: acrnetflixdevmac01.azurecr.io
  API_IMAGE_NAME: netflix-api
  WEB_IMAGE_NAME: netflix-web
  NAMESPACE: netflix-dev

jobs:
  build-and-deploy:
    name: Build, Push and Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Azure login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Login to Azure Container Registry
        run: az acr login --name ${{ secrets.ACR_NAME }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push API image
        run: |
          docker buildx build \
            --platform linux/arm64 \
            -f app/Dockerfile \
            -t $ACR_LOGIN_SERVER/$API_IMAGE_NAME:dev \
            --p            --p            --p            --p    sh             --p            --p          r build            --p       --platform linux/arm64 \
            -f ap   ockerfile.frontend \
                                              ME:dev                    h \
            app

      - name: Get AKS credentials
        run: |
                    -cr                    -cr        rc                    -cr E_RESOURCE_GROU                    -cr        ecrets.AKS_CLUSTER_NAME }} \
            --overwrite-existing

                ploy Kubernetes manifests
        run: |
          kubectl apply -f k8s/dev/namespace.yaml
          kubectl apply -f k8s/dev/a          kubectl apply -f k8s/dev/a          kubel

                                   o pull latest images
        run: |
          kube          kube          kument/netflix-  i -n $NAMESPACE
          kubectl rollout restart deployment/netflix-web -n $NAMES          kubectle: Wait for rollout
        run: |
          kubectl rollout status de          kubectl rollout status de          kubectl rollout status de          kubectl rollout status de          kubectl rollout status de          kubectl vice status
        run: |
          kubectl get pods -n $NAMESPACE
          kubectl get svc -n $NAMESPACE
